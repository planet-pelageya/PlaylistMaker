<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:background="@color/blue">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        tools:ignore="MissingConstraints">

        <TextView
            android:gravity="bottom"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/side"
            android:layout_marginTop="14dp"
            android:layout_marginEnd="210dp"
            android:layout_marginBottom="@dimen/side"
            android:text="@string/app_name"
            android:textColor="@color/white"
            android:textSize="@dimen/textSize" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/firstbutt"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingTop="80dp"
        >

        <Button
            android:id="@+id/firstbutton"
            android:layout_width="match_parent"
            android:layout_height="@dimen/zeropad"
            android:layout_marginStart="@dimen/side"
            android:layout_marginEnd="@dimen/side"
            android:layout_marginBottom="@dimen/eightpad"
            android:layout_weight="1"
            android:insetTop="@dimen/zeropad"
            android:insetBottom="@dimen/zeropad"
            android:text="@string/Search"
            android:textColor="@color/black"
            android:textSize="@dimen/textSize"
            app:backgroundTint="@color/white"
            app:cornerRadius="@dimen/side"
            app:icon="@drawable/search"
            app:iconGravity="textStart"
            app:iconPadding="@dimen/eightpad"
            app:iconTint="@color/black"
            android:textAllCaps="false" />

        <Button
            android:id="@+id/secondbutton"
            android:layout_width="match_parent"
            android:layout_height="@dimen/zeropad"
            android:layout_marginStart="@dimen/side"
            android:layout_marginTop="@dimen/eightpad"
            android:layout_marginEnd="@dimen/side"
            android:layout_marginBottom="@dimen/eightpad"
            android:layout_weight="1"
            android:insetTop="@dimen/zeropad"
            android:insetBottom="@dimen/zeropad"
            android:text="@string/mediateka"
            android:textColor="@color/black"
            android:textSize="@dimen/textSize"
            app:backgroundTint="@color/white"
            app:cornerRadius="@dimen/side"
            app:icon="@drawable/mediateka"
            app:iconGravity="textStart"
            app:iconTint="@color/black"
            app:iconPadding="@dimen/eightpad"
            android:textAllCaps="false"
            />

        <Button
            android:id="@+id/thirdbutton"
            android:layout_width="match_parent"
            android:layout_height="@dimen/zeropad"
            android:layout_weight="1"
            android:layout_marginStart="@dimen/side"
            android:layout_marginTop="@dimen/eightpad"
            android:layout_marginEnd="@dimen/side"
            android:layout_marginBottom="28dp"
            android:insetTop="@dimen/zeropad"
            android:insetBottom="@dimen/zeropad"
            android:text="@string/Settings"
            android:textColor="@color/black"
            android:textSize="@dimen/textSize"
            app:backgroundTint="@color/white"
            app:cornerRadius="@dimen/side"
            app:icon="@drawable/settings"
            app:iconGravity="textStart"
            app:iconTint="@color/black"
            app:iconPadding="@dimen/eightpad"
            android:textAllCaps="false" />


    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>